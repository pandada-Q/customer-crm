# ============================================================
#  客户资料管理助手 · 服务端环境配置模板
#  复制本文件为 .env 后按需填写，再启动服务：node server.js
#  修改后需重启服务才生效
# ============================================================

# —— DeepSeek 大模型（上传 .txt 自由文本时用它抽取联系人 / AI 小助手问答）——
# 1) 到 https://platform.deepseek.com 注册并创建 API Key（sk- 开头）
# 2) 把 Key 填到下面等号后面（不要加引号/空格），例如：DEEPSEEK_API_KEY=sk-xxxx
# 3) 保存本文件后重启服务即可，无需改代码。
# 不配置也能用：上传走「规则抽取」，AI 小助手降级为本地关键词检索（见 README）。
DEEPSEEK_API_KEY=

# 显式指定提供方（可选）。只要上面 KEY 非空，服务会自动启用 deepseek，无需填此项。
# LLM_PROVIDER=deepseek

# 可选进阶项（一般用默认即可）
# DEEPSEEK_MODEL=deepseek-chat
# DEEPSEEK_BASE_URL=https://api.deepseek.com

# —— 服务基础配置（可选，默认即如下）——
# 服务端口（默认 4173）
# PORT=4173
# 访问口令（强烈建议修改默认值 crm2026！单一口令鉴权，所有页面与 API 共用）
# CRM_PASSCODE=crm2026

# —— 测试工具开关（默认关闭，正式使用请保持关闭）——
# 仅当 =1 时：后端开放「清空数据」测试端点 /api/dev/clear，
# 前端顶栏显示「清空数据」按钮（用于反复上传测试，可一键清空全部业务数据，不可恢复）。
# DEV_TOOLS=0
