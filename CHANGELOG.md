# 变更记录（CHANGELOG）

本文件记录"客户资料管理助手"各版本变更。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本（[SemVer](https://semver.org/lang/zh-CN/)）。

## [v0.1.0] - 2026-09-05

首个可交付版本。围绕"把散落客户资料自动整理成结构化客户库"形成最小闭环，全程基于虚构样例素材（`input/`）开发与验证。

### Added（新增）

- **客户总表**：分页浏览；按姓名/公司/手机/邮箱/城市/备注检索；按城市/客户标签/合作状态筛选；联系人资料 dialog 编辑（含合作状态显式清空）；冲突联系人默认移出总表、拍板后回归
- **上传入库（异步任务）**：页面内拖拽/多文件上传；CSV / JSON / 自由文本（.txt，口语化微信记录）自动识别；任务逐文件处理、进度可见，处理中不阻塞总表查询
  - 自动分流：新联系人直入；命中既有无冲突自动合并；跨源字段冲突进待确认区（不再静默取一）
  - 去模式化：无需用户选择"新批次/更正清单"，系统自动判断（去重、合并或修正）
- **数据清洗与规则手册（`lib/rules.js` R1–R11）**：
  - CSV 列错位智能对齐（如公司名误加逗号 "深圳市创新,科技有限公司" 自动还原为完整公司名）
  - 无法置信对齐的坏行**不静默错位**，转为待确认低置信卡并附原始行（人工核对）
  - 手机号校验：11 位为合法；1 开头 7~10 位短号**保留原值**并提示"位数不足"（不静默丢弃）
  - 标签集合去重（如"客户/客户"→"客户"）；口语城市归一；非法邮箱/纯数字垃圾城市清理归位
  - 规则手册同时**注入 DeepSeek 抽取提示词**，保证 LLM 与规则抽取口径一致
- **待确认区**：冲突 / 合作意向初判 / AI 低置信三类卡片；卡片标注**来源文件**；采纳推荐 / 取 A / 取 B / 人工修改补全；拍板即时生效、全部留痕可撤销
- **意向客户**：列表（姓名/公司/电话/城市/合作状态/**跟进人**（含历史悬浮）/判断依据/来源）；行内「跟进」（登记跟进人+时间）/「移除」操作
- **待办事项**：素材中抽离的非联系人事项；状态机管理（待处理/完成/搁置/已移除）+ 带计数状态筛选 + 行内操作按钮（完成/搁置/移除/恢复，全部可逆）
- **上传处理记录**：右栏实时面板，每次上传每文件一条（时间/文件名/结果徽标），可展开处理明细，失败留痕
- **AI 数据小助手**：右下角悬浮对话框；**仅查询系统内数据**（确定性检索 → 系统内快照 → DeepSeek 作答）；无 Key 自动降级本地关键词检索；打开即见首句引导
- **审计留痕与撤销**：修改/拍板/跟进/移除/上传全留痕，可撤销
- **测试工具**：顶栏"清空数据"按钮（默认隐藏，需 `DEV_TOOLS=1` 启动才开放），便于反复上传测试
- **自动化测试**：32 条用例（规整管线断言 + 应用 API 端到端 + AI 助手检索/降级），`node:test` 实现、零第三方依赖

### Changed（变更）

- 合作状态口径统一为三枚举：有效 / 待定 / 暂停（口语描述自动归一，如"展会认识、可跟进"→待定）
- 上传 `.txt` 自由文本走 DeepSeek 抽取；结构化 CSV/JSON 走确定性解析（仅异常行可选送 LLM）
- 待办从"勾选完成"改为状态机（兼容旧数据：`done:true → done`，其余 → open，自动迁移）
- 页面布局：总表+上传左右分栏；表格自适应列宽（去固定最大宽度）
- 系统来源记录：待确认卡/联系人携带来源文件信息（新数据精确到文件，旧数据回退聚合来源）

### Fixed（修复）

- 修复合作状态过滤前端参数名与后端不一致（`coop` vs `cooperationStatus`）
- 修复存量重复标签（王强"客户/客户"→"客户"），上传合并路径同步去重并入标签
- 修复 10 位短号在入库前被清洗丢弃导致误报"缺少任何电话"（周杰案例，改为保留并精确提示）
- 修复 A 股式错误：意向页备注列重复拼接跟进信息（独立为跟进人列）
- 修复若干前端绑定区崩溃隐患（contactName 引用局部变量、按钮判空等）

### Removed（移除）

- 上传"新批次 / 更正清单"模式选择（改由系统自动分流）
- 待办勾选框与 `/api/todos/:id/toggle` 接口（替换为状态机 `/status`）

### 已知边界（本次未做，见 ROADMAP）

- 无多用户账号与角色权限（v0.2.0）；待办无到期日期（v0.3.0）；无定时推送（v0.4.0）
- 仅 CSV/JSON/TXT 导入，尚无 Excel（v0.5.0）
- AI 助手为只读文本推荐，不可跳转定位/直接改库（v0.6.0）
- 默认口令 `crm2026`，单机/内网部署请务必在 `.env` 修改（README「安全与配置说明」）

---

[CHANGELOG 维护约定]：每次发版在顶部新增版本条目；`Unreleased` 阶段随手记录，发版时整理到正式条目。
