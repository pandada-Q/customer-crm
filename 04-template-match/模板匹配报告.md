# 模板匹配报告：客户资料管理助手

> 匹配日期：2026-09-05
> 由 fde-template-match 生成，基于 03-spec/规格文档.md
> 知识库：`D:\Obsidian workspace\项目模板\`

## 1. 场景特征

从 03-spec.md 归纳的四个关键维度：

| 维度 | 内容 |
|------|------|
| 行业 | 小微企业客户资料管理（客户为不懂技术的小企业负责人，跨行业通用） |
| 流程环节 | 客户数据规整：多源（Excel/JSON/微信文本/老系统导出）采集 → 去重识别 → 字段冲突拍板 → 清洗归一 → 统一入库；配套公网应用持续承载（总表/待确认区/意向视图/待办/新批次上传） |
| AI 能力类型 | 结构化抽取（任意格式自由文本 → 目标 schema）、识别/分类（合作意向初判、待办事项提取）、抽取结果以「人工拍板/确认」闭环呈现 |
| 解决方案形态 | 混合项目：AI 数据规整管线 + 「静态前端 + 轻量后端 + 数据库」公网 Web 应用 + 持续「新批次投喂→合并→拍板」服务闭环 |

## 2. 比对过程

检索知识库 `templates/` 目录及 `index.md` 模板清单：

| 模板 | 匹配维度 | 匹配度 | 说明 |
|------|----------|--------|------|
| （无） | — | — | 知识库为空库：`templates/` 目录无任何 manifest，`index.md` 模板清单为「（暂无）」，无可比对模板 |

## 3. 匹配结论

**未命中**。理由：知识库当前为空库（尚无任何 fde-distill 沉淀的模板），无模板可与本项目的场景特征比对，故按新项目开展。

## 4. 初始化内容（仅命中时）

不适用——未命中，未复制任何文件（本 skill 只读知识库，未对 `D:\Obsidian workspace\项目模板\` 做任何写入）。

## 5. 下一步建议

未命中即按新项目开展：进入 fde-value-check 做商业价值评估。本项目完成后建议运行 fde-distill 沉淀为模板——其「多源脏乱客户数据规整 + 公网轻应用 + 人工拍板闭环」形态与 AI 能力组合具备可复制性（识别合作意向/待办提取可抽象为通用能力），沉淀后可显著加速后续同类客户资料治理项目。
